This is a tiny static frontend demo. It expects the backend on http://localhost:4000.
To serve static files quickly for development, install `serve` (npm i -g serve) then run `serve -s . -l 3000` inside this folder.
